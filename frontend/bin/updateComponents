#!/bin/bash

for f in "$@"
do
  echo "$f"
  if [ -e "$(dirname "$f")"/*.stories.tsx ]
  then
    # */*/*.stories.tsx
    p="$(dirname $f)"
    yarn updateComponent "$(basename "$p")" "$(basename "$(dirname "$p")")"
  fi
done
